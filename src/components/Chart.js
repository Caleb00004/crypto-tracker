// react-chartjs-2 is [A wrapper for Chart js that exist in react.]
// makes implementing chartjs easier in react.

export default function Chart () {
    return (
        <h4>The Chart component</h4>
    )
}